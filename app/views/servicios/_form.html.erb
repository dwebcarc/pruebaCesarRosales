<%= form_with(model: servicio) do |form| %>
  <% if servicio.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(servicio.errors.count, "error") %> prohibited this servicio from being saved:</h2>

      <ul>
        <% servicio.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :nombreServicio, style: "display: block" %>
    <%= form.text_field :nombreServicio %>
  </div>

  <div>
    <%= form.label :descripcionServicio, style: "display: block" %>
    <%= form.text_field :descripcionServicio %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
